import styled from 'styled-components'
import { colors } from '@/assets/styles/colors'

export const TableCardPredictionStyle = styled.table`
  width: 100%; //width: 29.9rem;
  //height: 14.6rem;
  background: ${colors.grey200};
  border-radius: 1.6rem;
  color: ${colors.grey800};
  padding: 1.6rem;
`
export const TableHeaderCardPrediction = styled.thead`
  color: ${colors.azul2};
  font-weight: 400;
`
